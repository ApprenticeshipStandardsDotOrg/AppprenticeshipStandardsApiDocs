# Occupations

## Get all occupations

```shell
curl "<%= config[:endpoint] %>/occupations"
```

> Example JSON response:

```json
{
    "data": [
        {
            "id": "34448da3-6260-4b90-ba5d-fb7d6f1cbbe6",
                "type": "occupations",
                "links": {
                    "self": "<%= config[:endpoint] %>/occupations/34448da3-6260-4b90-ba5d-fb7d6f1cbbe6"
                },
                "attributes": {
                    "title": "3D Printing Technician",
                    "onet_code": "17-3026.00",
                    "rapids_code": "2078",
                    "time_based_hours": 2000,
                    "competency_based_hours": 2000
                }
        },
        {
            "id": "e6fc1a85-a3ca-4b91-9314-0869c14346e7",
            "type": "occupations",
            "links": {
                "self": "<%= config[:endpoint] %>/occupations/e6fc1a85-a3ca-4b91-9314-0869c14346e7"
            },
            "attributes": {
                "title": "ALARM OPERATOR (Gov Serv)",
                "onet_code": "43-5031.00",
                "rapids_code": "0870",
                "time_based_hours": 2000,
                "competency_based_hours": 2000
            }
        }
    ]
}
```

This endpoint retrieves all occupations.

### HTTP Request

`GET <%= config[:endpoint] %>/occupations`

## Get a Specific Occupation

```shell
curl "<%= config[:endpoint] %>/occupations/2" \
  -H "Authorization: meowmeowmeow"
```

> The above command returns JSON structured like this:

```json
{
  "id": 2,
  "name": "Max",
  "breed": "unknown",
  "fluffiness": 5,
  "cuteness": 10
}
```

This endpoint retrieves a specific occupation.

<aside class="warning">Inside HTML code blocks like this one, you can't use Markdown, so use <code>&lt;code&gt;</code> blocks to denote code.</aside>

### HTTP Request

`GET <%= config[:endpoint] %>/occupations/<ID>`

### URL Parameters

Parameter | Description
--------- | -----------
ID | The ID of the kitten to retrieve
